%p#notice= notice

%p
  %img{src: @mock.url}
%p
  %b Posted by
  = @mock.creator_id

= render 'comments/form'

- if @mock.comments.any?
  %ul
    - @mock.comments.each do |comment|
      %li
        = comment.body
